# MDX Features (Manual Test Exercise)

This exercise exists to **manually test MDX rendering and workshop components** as
we develop the Epic Workshop packages.

Click **Start Learning** below to open the step, which contains examples of:

- callouts
- code-block metadata and line transforms
- `InlineFile`, `LinkToApp`, and `DiffLink`
- embedded videos
- mermaid diagrams

