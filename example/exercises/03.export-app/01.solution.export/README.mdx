# Export App

Here's the solution. Notice how the console output and exports are displayed in
the preview panel.

The `doubleNumbers` function uses the `map` method to transform each number in
the input array.
