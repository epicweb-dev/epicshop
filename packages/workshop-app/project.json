{
	"name": "@epic-web/workshop-app",
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"options": {
				"parallel": true,
				"cwd": "packages/workshop-app",
				"commands": ["bun run build:app", "bun run build:server"]
			}
		}
	}
}
