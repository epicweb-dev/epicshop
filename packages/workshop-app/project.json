{
	"name": "@epic-web/workshop-app",
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"options": {
				"parallel": true,
				"cwd": "packages/workshop-app",
				"commands": ["npm run build:app", "npm run build:server"]
			}
		}
	}
}
